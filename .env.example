# === POSTGRES ===
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=your_database_name
POSTGRES_PORT=5432

# === APP CONFIG ===
FRONTEND_PORT=3000

# === DATABASE CONNECTION === 
# Para Docker:
# DB_HOST=postgres_db
# DB_USER=${POSTGRES_USER}
# DB_PASS=${POSTGRES_PASSWORD}
# DB_NAME=${POSTGRES_DB}

# Para desarrollo local (sin Docker):
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_postgres_user        # Debe coincidir con POSTGRES_USER
DB_PASS=your_postgres_password    # Debe coincidir con POSTGRES_PASSWORD
DB_NAME=your_database_name        # Debe coincidir con POSTGRES_DB

# === ADMIN CREDENTIALS ===
ADMIN_USER=your_admin_username
ADMIN_PASSWORD=your_admin_password

# === NOTAS ===
# 1. Si usas Docker:
#    - Descomenta las variables con ${} y comenta las de desarrollo local
#    - Las referencias ${VARIABLE} funcionan en Docker Compose
#
# 2. Si desarrollas localmente (sin Docker):
#    - Usa las variables con valores directos (sin ${})
#    - Asegúrate que PostgreSQL esté instalado y corriendo
#    - Los valores DB_USER, DB_PASS y DB_NAME deben coincidir con los de POSTGRES_*

