"use client";

// Componente de layout para la página de calculadora
export default function CalculadoraLayout({ children }: { children: React.ReactNode }) {
	return (
		<div>
			{children}
		</div>
	);
}

