/* eslint-disable no-undef */
// Minimal .eslintrc to allow Next.js to detect the plugin during `next build`.
// This file coexists with the flat `eslint.config.js` used by editors/tools.
module.exports = {
  extends: ['plugin:@next/next/recommended'],
};
